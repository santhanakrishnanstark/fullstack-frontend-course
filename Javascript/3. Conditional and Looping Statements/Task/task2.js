// Task
// use for loop to iterate from 0 to 100 and print only odd numbers.

// create for loop to run from 0 to 100

for(let i=0; i<=100; i++) {

    // check if the current number is odd. use fromula  num % 2 != 0
    if(i % 2 != 0) {
        console.log('Odd Number: ', i);
    }
}